public static String reading(String fileName) throws IOException { String data; try (Scanner scanner = new Scanner(Paths.get(fileName), StandardCharsets.UTF_8.name()) { data = scanner.useDelimiter("\\A").next(); } return data; }